{
  "name": "hermes-zalo-control",
  "version": "0.1.0",
  "private": true,
  "description": "Hermes Zalo Control Center — web dashboard for monitoring and controlling AI agents that operate through Zalo",
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "packages/shared",
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:all": "concurrently -n backend,frontend,worker -c blue,green,yellow \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:worker\"",
    "dev:backend": "npm run dev -w packages/backend",
    "dev:frontend": "npm run dev -w packages/frontend",
    "dev:worker": "npm run dev:worker -w packages/backend",
    "build": "npm run build -w packages/shared && npm run build -w packages/backend && npm run build -w packages/frontend",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w packages/backend && npm run typecheck -w packages/frontend",
    "lint": "eslint \"packages/*/src/**/*.{ts,tsx}\"",
    "format": "prettier --write \"**/*.{ts,tsx,css,json,md}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,css,json,md}\" --ignore-path .gitignore",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:backend": "npm run test -w packages/backend",
    "test:shared": "npm run test -w packages/shared",
    "test:e2e": "vitest run packages/backend/src/__tests__/e2e",
    "db:generate": "npm run db:generate -w packages/backend",
    "db:migrate": "npm run db:migrate -w packages/backend",
    "db:seed": "npm run db:seed -w packages/backend",
    "db:studio": "npm run db:studio -w packages/backend",
    "db:guard": "npm run db:guard -w packages/backend",
    "db:guard:backup": "npm run db:guard:backup -w packages/backend",
    "db:push:safe": "npm run db:push:safe -w packages/backend",
    "backup:create": "npm run backup:create -w packages/backend",
    "backup:list": "npm run backup:list -w packages/backend",
    "backup:verify": "npm run backup:verify -w packages/backend",
    "backup:restore": "npm run backup:restore -w packages/backend",
    "restart:safe": "npm run restart:safe -w packages/backend",
    "clean": "rimraf packages/*/dist packages/*/.next packages/*/node_modules node_modules",
    "smoke": "echo '=== Health ===' && curl -s http://localhost:3000/api/health && echo '' && echo '=== Admin ===' && curl -s -u admin:dev-admin-password http://localhost:3000/api/admin/status && echo '' && echo '=== Zalo ===' && curl -s http://localhost:3000/api/zalo/status && echo '' && echo '=== Worker ===' && curl -s http://localhost:3000/api/worker/status && echo '' && echo '=== Parse ===' && curl -s -u admin:dev-admin-password -X POST http://localhost:3000/api/agent/parse-command -H 'Content-Type: application/json' --data-binary '{\"command\":\"22h nhac Le Phat vao group Lop Tu Hoc\"}' && echo ''"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}
